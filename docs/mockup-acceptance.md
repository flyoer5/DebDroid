# 模拟页验收核对清单（mockup ↔ requirements §4 FR）

> 用途：用户在模拟页验收时的对照表。每行给出 FR → 模拟页覆盖位置 → 如何验证。
> 状态：✅ 已覆盖可点验 · ⚠️ 部分覆盖（toast 级） · ❌ 缺失

## 向导

| FR | 覆盖 | 模拟页位置 | 验证方式 |
|---|---|---|---|
| FR-W1 首次启动进向导 | ✅ | `scr-wizard`（默认屏） | 打开 #wizard |
| FR-W2 安装进度/失败 | ✅ | `startInstall()` + `failInstall()` | 点「安装 Debian」看进度；点卡片下链接看失败错误卡与重试 |
| FR-W3 完成进入终端 | ✅ | `wiz-done` → `go('scr-terminal')` | 安装到 100% 后点「进入终端 ▸」 |
| FR-W4 中文默认 TUNA | ✅ | 完成卡文案"apt 源：清华 TUNA" | 完成态文案 |

## 终端与会话

| FR | 覆盖 | 模拟页位置 | 验证方式 |
|---|---|---|---|
| FR-T1 proot 启动 | ✅ | 终端命令流（tmux/apt/git/npm） | 看 #terminal |
| FR-T2 实时渲染/滚动 | ⚠️ | 静态命令流，终端区 overflow-y | 滚动终端内容 |
| FR-T3 29 键扩展键 | ✅ | `.extrakeys` 三排 29 键 | 点按键看 toast |
| FR-T4 8 配色+Nerd Font | ✅ | `scheme()` 跨屏联动 + 设置 Nerd Font 开关 | 设置点色块 → 回终端屏看配色变化 |
| FR-T5 唤醒锁 | ✅ | 保活组"唤醒锁"开关 | 开关切换 |
| FR-S1 新建会话 | ✅ | 终端菜单「＋ 新建会话」/ 抽屉「＋」 | 点击看 toast |
| FR-S2 抽屉会话选择/关闭 | ✅ | `drawer` 3 会话 + 全部关闭 | 开抽屉点会话 |
| FR-S3 tmux 保持 | ✅ | 命令流 `tmux new -A -s main` + 设置开关 | 终端命令流 + 设置「tmux 会话保持」 |
| FR-S4 自动恢复 | ✅ | 保活组「自动恢复会话」开关 | 开关切换 |
| FR-S5 启动失败 Toast | ✅ | 终端菜单「⚠ 模拟启动失败」 | 点击看红色错误 toast |

## 文件管理器

| FR | 覆盖 | 模拟页位置 | 验证方式 |
|---|---|---|---|
| FR-F1 双栏独立浏览 | ✅ | `.panes` L/R | 点目录/文件（R 栏 Download 有 9 项） |
| FR-F2 排序+书签+路径编辑 | ✅ | `fs-panel` 排序菜单 + `toggleStar` + `pathEdit` | 点 ⋮ 排序；点行尾 ☆；点路径条编辑 |
| FR-F3 批操作+删除确认 | ✅ | `batchbar` + `dlg-del` | 点选文件 → 批操作栏 → 删除 → 确认对话框 |
| FR-F4 权限串+图标细分 | ✅ | `.perm` 列 + 类型 emoji | 看列表 |
| FR-F5 编辑器入口 | ✅ | 点 .py/.md 文件 → `go('scr-editor')` | 点 server.py |

## 编辑器

| FR | 覆盖 | 模拟页位置 | 验证方式 |
|---|---|---|---|
| FR-E1 行号同步 | ✅ | `ed-gutter` 18 行 | 看行号列 |
| FR-E2 查找/替换 | ✅ | `findbar`（输入+计数+上/下/全部） | 点 🔍 或菜单「查找 / 替换」 |
| FR-E3 未保存拦截 | ✅ | `dlg-unsaved` 三按钮 | 点 ← 看对话框（保存并离开/不保存/取消） |
| FR-E4 撤销/只读/状态栏 | ⚠️ | 撤销/重做/跳行/换行为 toast；只读 `toggleRo` 真实切换；Ln/Col 状态栏真实 | 点 🔒 看只读；看底部状态栏 |

## SSH / 保活 / 系统

| FR | 覆盖 | 模拟页位置 | 验证方式 |
|---|---|---|---|
| FR-H1 SSH 即开即用 | ✅ | 设置「启用 SSH」+ 状态行"运行中" | 看设置 SSH 组 |
| FR-H2 真实状态/失败原因 | ✅ | `sshFail()` 展示 stderr | 点设置里"模拟端口占用" |
| FR-H3 端口/监听/密码/公钥/自启 | ✅ | `dlg-port`/`dlg-passwd`/`sshListen`/公钥行/自启开关 | 逐项点击 |
| FR-K1 前台服务 | ✅ | 保活「前台服务」开关 | 开关切换 |
| FR-K2 5 项开关 | ✅ | 保活组 5 开关 | 逐项切换 |
| FR-K3 电池白名单引导 | ❌ | 仅开关，无"点击跳系统设置页"演示 | —（建议后续在模拟页补引导路径） |
| FR-C1 镜像切换 | ✅ | `mirror()` 5 选 1 | 点镜像列表 |
| FR-C2 DNS + 恢复出厂 | ✅ | DNS 行 + `dlg-reset` 红色确认 → 回向导 | 点「恢复 Debian 出厂状态」 |
| FR-C3 设置持久化 | ✅ | toast"设置已保存" | 切换任意设置 |
| FR-C4 版本号 | ✅ | 设置底部 v2.0.0 (build 1) | 看设置系统组 |

## 汇总

- 已覆盖：✅ 27 条 · ⚠️ 2 条（FR-T2 滚动、FR-E4 部分功能为 toast 级）
- 缺失：❌ 1 条（FR-K3 电池白名单引导路径，仅影响演示完整性，不影响实现）

**待用户验收**：请打开 http://192.168.31.171:8802 按上表逐行点验，给出建议。
